"""
Mutable singleton used to assign global event ordering.
"""
type GlobalCounter @entity(immutable: false) {
	"""
	Singleton key `GLOBAL`.
	"""
	id: String!
	"""
	Latest subgraph-wide event ordering value assigned from this singleton.
	"""
	counter: BigInt!
}
