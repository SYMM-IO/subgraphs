"""
Single-row mutable counter used to assign deterministic subgraph-side ordering values.
"""
type GlobalCounter @entity(immutable: false) {
	"""
	Singleton counter ID, always GLOBAL in current handlers.
	"""
	id: String!
	"""
	Last subgraph ordering value assigned in the GLOBAL namespace.
	"""
	counter: BigInt!
}
