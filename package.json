{
  "name": "subgraph",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,graphql,yml,yaml,md}": "prettier --write"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "0.32.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "matchstick-as": "^0.5.0",
    "mustache": "^4.2.0",
    "prettier": "3.3.3"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.93.1"
  }
}
